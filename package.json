{
  "name": "psm-sawit-apk",
  "version": "1.41.0",
  "private": true,
  "description": "PSM SAWIT FIX: edit tanggal jam stabil, icon APK PSM, native saver gambar/backup, RAWBT, paket bersih siap upload GitHub.",
  "scripts": {
    "prepare:web": "node scripts/copy-html2canvas.js",
    "cap:add:android": "npx cap add android",
    "cap:sync:android": "npx cap sync android",
    "build:android:debug": "cd android && ./gradlew assembleDebug --stacktrace",
    "patch:android:icons": "node scripts/patch-android-icons.js"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "html2canvas": "1.4.1",
    "@capacitor/app": "^7.0.0",
    "@capacitor/app-launcher": "^7.0.0",
    "@capacitor/status-bar": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
